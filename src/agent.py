"""
Our entry-point agent. Stub for now — actual strategy lives in subsequent
iterations. Selected entry path (see wiki/strategies.md) is to fork the
robust_agent skeleton and gate shots through utils.aim_with_prediction.

Kaggle environment expects a top-level `def agent(obs, config=None)` that
returns a list of moves [[src_id, angle, ships], ...].
"""

from __future__ import annotations


def agent(obs, config=None):
    return []
